# ── Web Framework ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9          # file uploads
python-dotenv>=1.0.0

# ── Data Validation ───────────────────────────────────────────────────────────────
pydantic>=2.7.0

# ── Graph Database (in-process) ───────────────────────────────────────────────────
networkx>=3.2
numpy>=1.26.0
pymongo[srv]>=4.6.0
faiss-cpu>=1.14.0

# ── HTTP Client ───────────────────────────────────────────────────────────────────
httpx>=0.27.0                    # Cloudflare AI REST calls (persistent client)

# ── LLM / Embeddings (fallback only — primary is Cloudflare) ─────────────────────
openai>=1.30.0                   # fallback embeddings
anthropic>=0.27.0                # optional fallback LLM

# ── Token Counting ────────────────────────────────────────────────────────────────
tiktoken>=0.7.0                  # accurate token budget (replaces word-count hack)

# ── PDF Parsing ───────────────────────────────────────────────────────────────────
PyMuPDF>=1.24.0                  # best-in-class for legal/academic PDFs
pdfplumber>=0.11.0               # layout-aware table parser

# ── NLP / NER ─────────────────────────────────────────────────────────────────────
spacy>=3.7.0
# After install run: python -m spacy download en_core_web_lg


# ── OCR (optional fallback) ───────────────────────────────────────────────────────
pytesseract>=0.3.10
Pillow>=10.0.0
python-docx>=1.1.0

# ── AWS Textract (optional enterprise OCR) ────────────────────────────────────────
boto3>=1.34.0
